// Pool health thresholds
export const LOAN_EXPOSURE_WARNING_THRESHOLD = 50; // percent of pool in active loans
export const OUTSTANDING_COMMITMENTS_WARNING_COUNT = 3; // number of members with outstanding commitments

// Onboarding wizard version — bump this to re-show the tour for all users
export const CURRENT_WIZARD_VERSION = 1;
